# TODO

## 已完成

- [x] 支持 `--no-color`、`--test-ai`、`--help` 命令行参数
- [x] 将核心逻辑拆分为 `src/game/*` 与 `src/render/*` 目录模块
- [x] 固定 `160ms` 逻辑 tick，并增加 `16ms` 独立渲染帧
- [x] 超级食物功能（额外增长 `4` 节）
- [x] 炸弹功能（触碰即死，结算后移除）
- [x] 蛇死亡后拆成尸块，并逐段腐化成尸体食物
- [x] AI 需等待整批尸块腐化完成后再重生
- [x] 超级食物脉冲效果
- [x] 尸体食物生成闪光效果
- [x] 为核心规则、AI 避险、尸体腐化和渲染动画补单元测试
- [x] 死亡回放功能（玩家死亡后可浏览历史帧）

## 结构演进

- [x] 将蛇自身行为下沉到 `Snake`
  已包含 `advance`、`grows`、`projected_length`、控制模式与 AI 状态
- [x] 将 AI 决策状态收敛到蛇实体自身
  当前通过 `SnakeControl::Ai(AiState)` 维护随机漫步状态
- [x] 将世界规则按职责拆分到 `logic`、`ai`、`spawn`、`corpse`、`snake`
- [ ] 继续压缩 `GameState` 在 `logic.rs` 中的职责密度
  可评估再拆出 `collision.rs`、`items.rs` 或 `respawn.rs`
- [ ] 评估是否需要给 `GameEvent` 扩展更多渲染事件
  例如头撞头冲击、炸弹爆炸、分数变化提示
- [ ] 评估 `render` 层 API 是否还需要进一步收敛
  当前 `board`、`panels`、`animation` 已拆开，但状态同步仍集中在 `RenderState`

## 视觉效果优化

- [ ] 蛇身使用更饱满的方块字符（如 `■` / `□`）替代当前 `o` / `a`
- [ ] 为尸块和尸体食物增加更明确的视觉区分
- [ ] 调整标题与状态栏层次，让主界面信息密度更高但更易扫读
- [ ] 为 `--no-color` 模式设计更明显的字符级区分方案

## 动画效果构想

- [x] 超级食物脉冲
- [x] 尸体食物生成闪光
- [ ] 普通食物被吃掉时增加吃到反馈闪烁
- [ ] 炸弹危险闪烁
- [ ] 暂停和 `Game Over` 弹窗淡入
- [ ] 分数跳字或状态栏短时高亮
- [ ] 头撞头冲击闪
- [ ] 更明显的死亡碎裂动画
- [ ] 蛇移动补间或头部滑行感

## 测试覆盖

- [x] AI 行为与安全性回归
- [x] 食物补货与尸体食物流转逻辑
- [x] 蛇身增长逻辑
- [x] 碰撞检测边界条件
- [x] 尸块腐化与 AI 延迟重生
- [x] 渲染层脉冲与闪光动画
- [ ] 为 `app.rs` 的输入映射补更细的测试
- [ ] 为窗口 resize 触发的重开流程补回归测试
- [ ] 为状态栏/帮助栏文案补轻量快照测试
